{% extends "base.njk" %}

{% block style %}
	#{{ title | slugify }} {
		background-color: yellow;
	}

	{% for tag in tags %}
	#{{ tag | slugify }} {
		background-color: yellow;
	}
	{% endfor %}
{% endblock %}

{% block subnav %}
  <h2 class="subnav-title">games</h2>
    <p class="subnav-category">web games</p>
      {% for project in collections.browser %}
      <div class="nav-item" id="{{ project.data.title | slugify }}"><a href="{{ project.url }}">{{ project.data.title }}</a></div>
      {% endfor %}
      <p class="subnav-category">bitsy</p>
      {% for project in collections.bitsy %}
      <div class="nav-item" id="{{ project.data.title | slugify }}"><a href="{{ project.url }}">{{ project.data.title }}</a></div>
      {% endfor %}
{% endblock %}

{% block content %}

	<div id="crumbs">
	<a href="/">•</a> / <a href="/{{ category }}">{{ category }}</a> / {{ title }}
	</div>

	<h1>{{ title }}</h1>
	
	{{ content | safe }}
	
	<hr>
	<h1 style="text-align: center">✷</h1>
	
	<div id="tagrow">
	{% for tag in tags %}
		<span class="tag">{{ tag }}</span>
	{% endfor %}
	</div>
{% endblock %}
