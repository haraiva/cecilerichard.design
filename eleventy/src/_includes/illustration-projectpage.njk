{% extends "base.njk" %}

{% block style %}
	#{{ title | slugify }} {
		background-color: var(--colHighlight);
	}

	{% for tag in tags %}
	#{{ tag | slugify }} {
		background-color: var(--colHighlight);
	}
	{% endfor %}
{% endblock %}

{% block subnav %}
  <h2 class="subnav-title">illustration</h2>
    <p class="subnav-category">drawings</p>
      {% for project in collections.drawings %}
      <div class="nav-item" id="{{ project.data.title | slugify }}"><a href="{{ project.url }}">{{ project.data.title }}</a></div>
      {% endfor %}
    <p class="subnav-category">game art</p>
      {% for project in collections['game art'] %}
      <div class="nav-item" id="{{ project.data.title | slugify }}"><a href="{{ project.url }}">{{ project.data.title }}</a></div>
      {% endfor %}
    <p class="subnav-category">zines</p>
      {% for project in collections.zines %}
      <div class="nav-item" id="{{ project.data.title | slugify }}"><a href="{{ project.url }}">{{ project.data.title }}</a></div>
      {% endfor %}
{% endblock %}

{% block content %}

	<div id="crumbs">
	<a href="/">•</a> / <a href="/{{ category }}">{{ category }}</a> / {{ title }}
	</div>

	<h1>{{ title }}</h1>
	
	{{ content | safe }}
	
	<hr>
	<h1 style="text-align: center">✷</h1>
	
	<div id="tagrow">
	{% for tag in tags %}
		<span class="tag">{{ tag }}</span>
	{% endfor %}
	</div>
{% endblock %}
